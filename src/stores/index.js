import { createPinia } from 'pinia';

// Создаем и экспортируем экземпляр Pinia
const pinia = createPinia();

export default pinia;

// Экспортируем все stores для удобного импорта
// export * from './formStore';
